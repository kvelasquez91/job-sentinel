requests>=2.31,<3
beautifulsoup4>=4.12,<5
lxml>=6.0,<7
lxml-html-clean>=0.4
fastapi>=0.110,<1
uvicorn>=0.27,<1
jinja2>=3.1,<4
pyyaml>=6.0,<7
python-dotenv>=1.0,<2
httpx>=0.27
tenacity>=8.2
aiosqlite>=0.20
yfinance>=0.2.36
# LLM scoring + resume tailoring call the local `claude` CLI (Claude Code) in headless
# mode, billed to your Claude subscription. Install Claude Code separately and run
# `claude auth status` to confirm login. No Python SDK / ANTHROPIC_API_KEY required.

# Testing
pytest>=8.0

# Resume tailor — Google Docs/Drive API + web extraction
google-api-python-client>=2.193.0
google-auth>=2.49.1
google-auth-httplib2>=0.3.1
google-auth-oauthlib>=1.3.1
trafilatura>=2.0.0
pypdf>=3.0.0
pdfplumber>=0.11.0
